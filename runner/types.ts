type OutData = {
  noPushSimple?: Array<number>,
  pushSimple?: Array<number>
};

type Timings = {
  extendedInfo: {
    value:{
      name: string,
      duration: number}[]
  }};

export {OutData, Timings}
